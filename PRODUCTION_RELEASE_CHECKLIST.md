# 正式上线基础工作与安全检查清单

本文档用于本项目第一阶段发布正式版本前的上线准备、发布验证和安全检查。项目当前采用 Docker Compose 部署，后端为 NestJS + Prisma + PostgreSQL + Redis + MinIO，前端为 Vue 3 + Nginx，移动端为 Uni-app。

## 1. 上线前基础工作

### 1.1 发布范围确认

- 明确本次上线范围：后台管理端、商城 API、移动端 H5/小程序、文件存储、打印相关能力是否全部对外开放。
- 冻结发布分支：建议继续沿用现有策略，`main` 用于测试环境，`master` 用于正式环境。
- 整理发布说明：包含版本号、核心功能、已知限制、数据库迁移说明、回滚方案。
- 完成测试环境验收：订单、支付、库存、优惠券、余额、退货退款、文件上传、打印配置等主链路必须走通。

### 1.2 域名、HTTPS 与网络入口

- 准备正式域名：
  - 管理后台域名，例如 `admin.example.com`
  - 商城 API 域名，例如 `api.example.com`
  - 文件访问域名，例如 `files.example.com` 或复用 API 域名
  - 移动端 H5 域名，例如 `m.example.com`
- 配置 HTTPS 证书，禁止正式环境裸 HTTP 访问。
- 建议在公网入口前增加反向代理或网关，例如 Nginx、Caddy、云负载均衡。
- 只对外暴露必要端口：
  - 80/443：公网入口
  - 22：SSH，建议限制来源 IP
  - 不直接暴露 PostgreSQL、Redis、MinIO Console
- 正式环境 `FILE_BASE_URL`、前端 `VITE_API_BASE_URL`、移动端 `VITE_API_BASE_URL` 必须替换为正式 HTTPS 域名。

### 1.3 服务器与运行环境

- 准备独立正式服务器或云资源，不与开发环境混用。
- 安装并锁定基础组件版本：
  - Docker
  - Docker Compose
  - Git
  - 系统时区与时间同步服务
- 配置服务器防火墙和安全组，只放行必要端口。
- 配置磁盘容量监控，重点关注：
  - PostgreSQL 数据卷
  - MinIO 对象存储数据卷
  - Redis AOF 数据
  - Docker 镜像和容器日志
- 配置日志轮转，避免容器日志撑满磁盘。

### 1.4 环境变量与密钥

正式环境必须在服务器 `.env` 或 Secret 管理系统中注入敏感配置，不要把真实密钥提交到 Git。

必须替换：

- `DB_PASSWORD`
- `DATABASE_URL`
- `JWT_SECRET`
- `JWT_REFRESH_SECRET`，建议单独配置，不要复用 `JWT_SECRET`
- `SYSTEM_SETTINGS_ENCRYPTION_KEY`，必须与 JWT 密钥独立，用于加密数据库中的微信密钥/私钥
- `REDIS_PASSWORD`
- `MINIO_ROOT_USER`
- `MINIO_ROOT_PASSWORD`
- `MINIO_ACCESS_KEY`
- `MINIO_SECRET_KEY`
- 微信小程序 `AppId`、`AppSecret`
- 微信支付商户号、APIv3 Key、证书、回调地址

当前项目需要特别注意：

- `docker-compose.yaml` 中存在本地开发默认密码和示例 `JWT_SECRET`，正式环境必须通过 `docker-compose.production.yaml` 和服务器 `.env` 覆盖。
- `docker-compose.production.yaml` 已对数据库、Redis、JWT、系统设置加密密钥和 MinIO 敏感变量执行 fail-fast；缺少任一项都不应启动正式环境。
- `backend/.env.production`、`frontend/.env.production`、`mobile/.env.production` 当前是模板性质，正式构建和部署时必须覆盖为真实生产配置。

### 1.5 数据库迁移与种子数据

- 上线前在测试环境完整执行迁移，并验证 Prisma schema 与数据库一致。
- 正式环境首次部署：
  - 先备份空库或确认无历史数据。
  - 执行 Prisma migration。
  - 按需执行 seed，但不要保留默认弱口令。
- 如果正式环境已经有数据：
  - 禁止直接使用会重建表或清空数据的命令。
  - 发布前先导出备份。
  - 在测试库用正式数据脱敏副本演练迁移。
- 发布后检查关键表数据：
  - 管理员账号、角色、菜单权限
  - 商品、SKU、库存
  - 系统设置
  - 支付配置
  - MinIO bucket 与文件访问

### 1.6 管理员账号与权限初始化

- 删除或修改默认管理员密码 `admin / 123456`。
- 为超级管理员设置强密码，并只分配给必要人员。
- 创建日常运营角色，避免所有人都使用超级管理员。
- 检查菜单和按钮权限，确认普通账号不能访问系统管理、财务配置、支付配置、数据库相关接口。
- 建议建立账号交接流程：离职、换岗、外包账号必须及时禁用。

### 1.7 文件存储与静态资源

- 正式 MinIO bucket 应独立于测试环境。
- 配置 bucket 访问策略：公开读仅限确实需要公网访问的商品图、评价图、Banner 图。
- 不要把支付证书、私密合同、导入模板中的敏感数据放入公开 bucket。
- 明确文件清理策略：无引用上传文件、过期临时文件、失败导入文件。
- 配置对象存储备份，至少覆盖商品图片、评价图片、打印模板相关资源。

### 1.8 移动端与第三方平台配置

- 微信小程序：
  - 配置正式服务器域名、业务域名、上传下载域名。
  - 确认 TLS 证书、备案、类目、隐私协议。
  - 使用正式 `AppId` 和 `AppSecret`。
- 微信支付：
  - 配置正式商户号、APIv3 Key、证书、回调地址。
  - 回调地址必须使用 HTTPS。
  - 支付回调接口应具备签名验签、幂等处理、金额校验。
- 短信、物流、地图、客服等第三方服务如后续启用，也必须使用正式环境密钥和白名单。

### 1.9 CI/CD 与发布流程

- GitHub Actions Secrets 必须完整配置：
  - `SERVER_HOST`
  - `SERVER_USER`
  - `SSH_PRIVATE_KEY`
  - `SSH_TARGET_DIR`
- 正式环境 GitHub Environment 必须开启人工审批。
- 发布前检查 CI：
  - 后端测试通过
  - 后端构建通过
  - 前端构建通过
  - Docker 镜像构建通过
- 发布步骤建议：
  1. 合并到 `main` 并部署测试环境。
  2. 在测试环境完成验收。
  3. 合并到 `master`。
  4. 审批正式部署。
  5. 部署后执行线上冒烟测试。

### 1.10 备份与恢复

必须上线前完成恢复演练，不能只配置备份任务。

- PostgreSQL：
  - 每日至少一次全量备份。
  - 关键业务高峰期可增加增量或 WAL 归档。
  - 备份文件加密存储，保留 7 到 30 天。
- MinIO：
  - 定期同步到异地对象存储或备份盘。
  - 确认图片和附件可恢复。
- Redis：
  - 当前 Redis 开启 AOF，仍需明确是否保存业务关键状态。
  - 如果 Redis 只做缓存，重点验证重启后业务可恢复。
- 恢复演练：
  - 新机器恢复数据库。
  - 恢复 MinIO 数据。
  - 启动应用并完成登录、下单、支付回调模拟、库存查询。

### 1.11 监控、日志与告警

- 服务存活监控：
  - 前端入口
  - 后端 `/auth/me` 或专用健康检查接口
  - PostgreSQL
  - Redis
  - MinIO
- 业务指标监控：
  - 登录失败次数
  - 下单失败次数
  - 支付成功率和回调失败数
  - 库存扣减失败数
  - 退款失败数
  - 文件上传失败数
- 资源监控：
  - CPU、内存、磁盘、网络
  - 数据库连接数
  - Docker 容器重启次数
- 日志：
  - 保留后端应用日志、Nginx 访问日志、错误日志、数据库慢查询日志。
  - 日志中不得输出密码、Token、支付密钥、身份证、银行卡等敏感字段。

## 2. 安全检查清单

### 2.1 后端接口安全

- 生产环境限制 CORS 来源，不应使用 `app.enableCors()` 的完全开放默认配置。
- 生产环境关闭或保护 Swagger：
  - 当前 `backend/src/main.ts` 会开放 `/api/docs`、`/api/docs/admin-json`、`/api/docs/mall-json`。
  - 正式环境建议关闭，或加 Basic Auth、IP 白名单、VPN 访问限制。
- 全局输入校验已经启用 `ValidationPipe`，建议补充：
  - `forbidNonWhitelisted: true`
  - DTO 中补齐长度、格式、枚举、数值范围校验。
- 对登录、注册、短信、兑换码、支付回调等接口增加限流。
- 对后台管理接口统一校验 JWT 和角色权限。
- 确认所有按用户查询的数据都带当前用户或客户维度过滤，避免越权读取订单、地址、余额、优惠券。
- 文件上传接口必须校验：
  - 文件类型
  - 文件大小
  - 扩展名与 MIME
  - 上传目录
  - 是否允许脚本文件
- 对导入、富文本、打印模板等功能防范 XSS 和模板注入。

### 2.2 认证与密码安全

- `JWT_SECRET` 至少 32 字节随机字符串，建议 64 字节以上。
- `JWT_REFRESH_SECRET` 独立配置。
- 缩短 Access Token 有效期，Refresh Token 设置合理过期时间。
- 登录失败增加限流或账号临时锁定。
- 管理员密码必须满足复杂度要求。
- 禁止默认密码上线。
- 用户密码只允许 bcrypt/argon2 等哈希存储，禁止明文日志和响应返回。
- 管理后台建议启用二次验证或限制办公 IP。

### 2.3 数据库安全

- PostgreSQL 正式环境不要暴露到公网。
- 使用强密码，不使用 `postgres/postgres`。
- 应用连接数据库建议使用专用低权限用户，不直接使用超级用户。
- 定期备份并加密。
- 开启慢查询日志，用于排查库存、订单、财务统计类性能问题。
- 定期检查迁移脚本，避免破坏性 DDL 未经审批进入正式环境。

### 2.4 Redis 安全

- Redis 不暴露公网端口。
- 设置强 `REDIS_PASSWORD`，不要使用默认 `redis123`。
- 如部署在不可信网络，使用内网或 TLS 隧道。
- 禁止使用危险命令或通过防火墙限制访问来源。
- 监控 Redis 内存，避免缓存击穿或异常写入导致 OOM。

### 2.5 MinIO 与文件安全

- MinIO Console 不对公网开放。
- Root 用户只用于初始化，应用使用独立 Access Key。
- Access Key 权限最小化，只允许访问指定 bucket。
- bucket 不要整体公开写。
- 上传文件名不要直接信任用户输入，避免路径穿越和覆盖。
- 图片访问建议使用独立文件域名，并限制可执行内容。

### 2.6 支付与财务安全

- 微信支付回调必须验签，并校验：
  - 商户号
  - 订单号
  - 金额
  - 支付状态
  - 回调幂等
- 余额支付、充值、退款必须使用数据库事务。
- 财务相关接口必须有操作日志。
- 后台手工调账、退款、作废优惠券等高风险操作建议增加二次确认和审计。
- 不要在系统设置接口中向前端返回完整支付密钥或微信 AppSecret。

### 2.7 库存与订单一致性安全

- 商城 C 端当前已有库存锁定机制，上线前必须重点压测：
  - 多用户同时加购和下单
  - 支付超时取消
  - 订单取消释放库存
  - 发货扣减库存
  - 退款和退货回滚
- B 端订单当前未统一锁库存，存在“下单成功但发货时库存不足”的体验风险。正式上线前至少要在运营 SOP 中说明；如果 B 端并发下单频繁，建议优先补齐 B 端锁库存。
- 定期巡检库存不变量：
  - `available = quantity - locked`
  - `quantity >= locked`
  - `locked >= 0`
  - `available >= 0`

### 2.8 前端与移动端安全

- 前端构建产物中不能包含真实密钥。`VITE_*` 变量会进入浏览器，不能放私密信息。
- 管理后台路由守卫只能作为体验层控制，后端权限校验才是安全边界。
- 避免在 localStorage 中长期保存高权限 Token；如继续使用，需要缩短有效期并配合刷新机制。
- 富文本详情、评价内容、打印模板预览需要做 XSS 防护。
- 移动端不要在包内硬编码 AppSecret、支付密钥、管理员账号。

### 2.9 Nginx 与响应头

建议在正式入口增加安全响应头：

- `Strict-Transport-Security`
- `X-Content-Type-Options: nosniff`
- `X-Frame-Options: DENY` 或按业务设置
- `Referrer-Policy`
- `Content-Security-Policy`

同时配置：

- 上传大小限制。
- API 超时时间。
- 静态资源缓存策略。
- 访问日志脱敏。
- 反向代理只转发必要路径。

### 2.10 服务器 SSH 与运维安全

- 禁止 root 密码登录。
- 使用 SSH Key 登录。
- 限制 SSH 来源 IP。
- 定期更新系统安全补丁。
- 最小化服务器用户权限。
- GitHub Actions 部署 Key 只授予必要仓库和必要权限。
- 生产服务器 `.env` 文件权限建议为 `600`。

## 3. 上线前必须处理的当前项目风险点

以下项建议作为正式发布阻断项处理：

- 修改默认管理员密码，禁用或重置 `admin / 123456`。
- 生产环境关闭或保护 `/api/docs`、`/api/docs/admin-json`、`/api/docs/mall-json`。
- 将 CORS 改为正式域名白名单。
- 确认正式环境所有密钥已覆盖默认值，尤其是 JWT、Redis、MinIO、数据库密码。
- 确认 PostgreSQL、Redis、MinIO Console 不对公网开放。
- 配置 HTTPS，前端、后端、文件访问、支付回调全部使用 HTTPS。
- 完成数据库和 MinIO 的备份恢复演练。
- 完成支付回调验签、幂等和金额校验验证。
- 完成库存高并发下单、取消、发货、退款链路测试。
- 确认生产构建使用正式 API 地址，不再指向 `localhost`。

## 4. 正式发布后冒烟测试

发布完成后立即验证：

- 管理后台可以登录，普通账号和管理员权限隔离正常。
- 商品分类、品牌、SPU、SKU、库存查询正常。
- 文件上传和图片访问正常。
- 商城商品列表、详情、搜索、购物车正常。
- 下单、库存锁定、取消订单释放库存正常。
- 支付流程可以创建支付单，回调处理正常。
- 余额支付、充值、退款流水正常。
- 优惠券领取、使用、核销正常。
- 发货、退货、库存流水正常。
- MinIO、PostgreSQL、Redis 容器重启后应用可恢复。
- 监控和告警能收到测试告警。

## 5. 回滚预案

- 发布前记录当前镜像版本、Git commit、数据库迁移版本。
- 发布前完成数据库备份和 MinIO 备份。
- 如果只涉及前后端代码：
  - 回退到上一版本镜像或上一 commit 重新部署。
- 如果涉及数据库迁移：
  - 优先使用兼容性迁移，避免必须回滚 schema。
  - 如已写入新结构数据，必须按预案脚本修复或从备份恢复。
- 如果支付、库存、财务链路出现异常：
  - 立即暂停下单或支付入口。
  - 保留日志和数据库现场。
  - 对异常订单进行人工核对后再恢复服务。

## 6. 建议上线后持续改进

- 增加专用健康检查接口。
- 增加接口限流和登录失败锁定。
- 增加 OpenTelemetry 或 APM 链路追踪。
- 增加数据库慢查询分析。
- 增加对象存储生命周期清理。
- 增加 B 端订单库存锁定能力。
- 增加订单、支付、库存、余额的定时对账任务。
- 增加生产只读审计账号，降低直接操作数据库风险。
